language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t jaumealg77/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true jaumealg77/docker-react npm run test   